server:
  port: 8888
spring:
  application:
    name: erudio-config-server
  cloud:
    config:
      server:
        git:
          uri: https://github.com/JulianaR23/Microservices-config-server
          default-label: main
          search-paths:
          - 'greeting-service*'